// import đối tượng express trong Express JS
import express from "express";

// tạo đối tượng router trong Express JS
const router = express.Router();

// Định nghĩa route POST /api/user/register
// router.post("/register", registerUser);

export default router;
